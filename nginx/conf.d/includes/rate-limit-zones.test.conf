# Rate limiting relaxado para testes de integração e carga
limit_req_zone $binary_remote_addr zone=api_general:10m rate=500r/s;
limit_req_zone $binary_remote_addr zone=auth_sensitive:10m rate=200r/s;
limit_req_zone $binary_remote_addr zone=mapbox_api:10m rate=200r/s;
limit_req_zone $binary_remote_addr zone=upload_api:10m rate=100r/s;
limit_conn_zone $binary_remote_addr zone=conn_per_ip:10m;
