events {}

http {
    client_max_body_size 10M;

    # Inclui apenas app.conf; upstreams foram movidos para resolver em runtime (evita "host not found" na subida)
    include /etc/nginx/conf.d/*.conf;
}
